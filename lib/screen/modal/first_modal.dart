import 'package:flutter/material.dart';

class FirstModal extends StatelessWidget {
  const FirstModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FirstModal'),
      ),
    );
  }
}
